<?xml version="1.0" encoding="utf-8"?>
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/dark_music_player"
    tools:context=".ui.fragment.DeviceFragment">
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">
        <EditText
            android:id="@+id/sv_device_songs"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_alignParentTop="true"
            android:background="@drawable/sv_style"
            android:layout_marginHorizontal="30dp"
            android:layout_marginVertical="25dp"
            android:paddingVertical="5dp"
            android:paddingHorizontal="10dp"
            android:textSize="21sp"
            android:maxLines="1"/>
        <ImageView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignTop="@id/sv_device_songs"
            android:layout_alignBottom="@id/sv_device_songs"
            android:layout_alignEnd="@id/sv_device_songs"
            android:background="@color/dark_music_player"
            android:layout_margin="10dp"
            android:src="@drawable/search"
            android:scaleType="centerInside"/>
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rv_song_list"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_below="@id/sv_device_songs"
            app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
            tools:listitem="@layout/item_song_list"/>
    </RelativeLayout>
</FrameLayout>